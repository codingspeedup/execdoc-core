package xp.kdm.code;

public class Implements extends AbstractCodeRelationship<CodeItem, CodeItem> {

    public Implements(CodeModel kdmModel, CodeItem from, CodeItem to) {
        super(kdmModel, from, to);
    }

}
